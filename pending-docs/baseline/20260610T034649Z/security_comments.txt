backend/provision/fastly_api.py:92:    # Security: scrub internal-routing headers a client could spoof.
backend/provision/orchestrator.py:513:            # Security: ``bucket`` is supplied via the provisioning
backend/routers/alerts.py:92:    # Security: analyst can only preview alerts against their scoped
backend/routers/alerts.py:198:    # Security: pre-flight scope check BEFORE the mutation. Earlier
backend/routers/admin.py:413:    # Security: ``os.path.join(base, key)`` returns ``key`` when
backend/routers/provision.py:229:            # Security: do NOT fall back to the server-stored
backend/routers/provision.py:252:    # Security: destructive teardown (logging / CDN / bucket) requires a
backend/routers/services/core.py:618:# Security: was @router.get — moved to POST/PATCH so a cross-origin
backend/routers/views.py:57:    # Security: pre-flight scope check, mirrors alerts.delete_alert.
backend/core/ingest.py:551:                # Security: escape single quotes in each local path before
backend/core/ingest.py:573:                            # Security: per-file isolation read also needs escaping.
backend/core/ingest.py:612:                    # Security: same escaping treatment for the
backend/core/ingest.py:643:                    # Security: consistent escape helper across the
backend/core/ingest.py:724:                            # Security: corrupt-file diagnostic path
backend/core/log_fields.py:848:    # Security: each origin-metric field interpolates the value of a
backend/core/log_fields.py:1493:                # Security: keep the substr cap even when generating the
backend/core/fastly/utils.py:234:  # Security: hash on the full URL (path + query string), not just
backend/core/iceberg/_core.py:3288:            # Security: validate via isoparse before interpolation. Without
backend/core/share_db/connection.py:75:        # Security: ``DatabaseError`` is the parent of
backend/scoring/scorer.py:50:# Security: was 0.5 — there was a "robotic safe zone" between
backend/scoring/scorer.py:65:# Security: tampered cookies are a strictly stronger anomaly signal
